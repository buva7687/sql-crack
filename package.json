{
  "name": "sql-crack",
  "displayName": "SQL Crack",
  "description": "Visualize SQL queries with interactive flow diagrams",
  "version": "0.3.8",
  "license": "MIT",
  "publisher": "buvan",
  "repository": {
    "type": "git",
    "url": "https://github.com/buva7687/sql-crack.git"
  },
  "icon": "assets/icon.png",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "keywords": [
    "sql",
    "visualization",
    "diagram",
    "flow",
    "lineage",
    "query",
    "database"
  ],
  "activationEvents": [
    "onLanguage:sql",
    "onCommand:sql-crack.visualize",
    "onCommand:sql-crack.analyzeWorkspace",
    "onCommand:sql-crack.showWorkspaceUxMetrics"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "sql-crack.visualize",
        "title": "SQL Crack: Visualize SQL Query",
        "icon": {
          "light": "assets/sql-flow-sc-light.svg",
          "dark": "assets/sql-flow-sc-dark.svg"
        }
      },
      {
        "command": "sql-crack.analyzeWorkspace",
        "title": "SQL Crack: Analyze Workspace Dependencies",
        "icon": "$(references)"
      },
      {
        "command": "sql-crack.showWorkspaceUxMetrics",
        "title": "SQL Crack: Show Workspace UX Metrics",
        "icon": "$(graph-line)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "sql-crack.visualize",
          "when": "resourceExtname == .sql || editorLangId == sql || sqlCrack.isAdditionalSqlFile",
          "group": "navigation"
        }
      ],
      "editor/context": [
        {
          "command": "sql-crack.visualize",
          "when": "resourceExtname == .sql || editorLangId == sql || sqlCrack.isAdditionalSqlFile",
          "group": "navigation"
        }
      ],
      "explorer/context": [
        {
          "command": "sql-crack.visualize",
          "when": "resourceExtname == .sql || sqlCrack.isAdditionalSqlFile",
          "group": "navigation"
        },
        {
          "command": "sql-crack.analyzeWorkspace",
          "when": "explorerResourceIsFolder",
          "icon": "$(references)",
          "group": "navigation"
        }
      ],
      "commandPalette": [
        {
          "command": "sql-crack.visualize",
          "when": "editorLangId == sql || sqlCrack.isAdditionalSqlFile",
          "group": "navigation"
        },
        {
          "command": "sql-crack.analyzeWorkspace",
          "when": "workspaceFolderCount > 0"
        },
        {
          "command": "sql-crack.showWorkspaceUxMetrics",
          "when": "workspaceFolderCount > 0"
        }
      ]
    },
    "keybindings": [
      {
        "command": "sql-crack.visualize",
        "key": "ctrl+shift+l",
        "mac": "cmd+shift+l",
        "when": "resourceExtname == .sql || editorLangId == sql || sqlCrack.isAdditionalSqlFile"
      }
    ],
    "configuration": {
      "title": "SQL Crack",
      "properties": {
        "sqlCrack.defaultDialect": {
          "type": "string",
          "default": "MySQL",
          "enum": [
            "MySQL",
            "PostgreSQL",
            "TransactSQL",
            "SQL Server",
            "Snowflake",
            "BigQuery",
            "Redshift",
            "Hive",
            "Athena",
            "Trino",
            "MariaDB",
            "SQLite",
            "Oracle",
            "Teradata"
          ],
          "enumDescriptions": [
            "MySQL dialect",
            "PostgreSQL dialect",
            "SQL Server / T-SQL dialect",
            "SQL Server / T-SQL dialect (alias for TransactSQL)",
            "Snowflake dialect",
            "Google BigQuery dialect",
            "Amazon Redshift dialect",
            "Apache Hive / Databricks dialect",
            "Amazon Athena dialect",
            "Trino (formerly Presto) dialect",
            "MariaDB dialect",
            "SQLite dialect",
            "Oracle Database dialect",
            "Teradata dialect"
          ],
          "description": "Default SQL dialect for parsing queries"
        },
        "sqlCrack.syncEditorToFlow": {
          "type": "boolean",
          "default": true,
          "description": "Highlight flow nodes when clicking in SQL editor"
        },
        "sqlCrack.viewLocation": {
          "type": "string",
          "default": "tab",
          "enum": [
            "beside",
            "tab"
          ],
          "enumDescriptions": [
            "Opens beside the SQL file (like Markdown preview)",
            "Opens as a new tab in the editor area (default)"
          ],
          "description": "Where to display the SQL visualization panel"
        },
        "sqlCrack.defaultLayout": {
          "type": "string",
          "default": "vertical",
          "enum": [
            "vertical",
            "horizontal",
            "compact",
            "force",
            "radial"
          ],
          "enumDescriptions": [
            "Vertical layout (top-to-bottom flow)",
            "Horizontal layout (left-to-right flow)",
            "Compact layout (tighter spacing)",
            "Force-directed layout (physics-based positioning)",
            "Radial layout (root at center, levels as rings)"
          ],
          "description": "Default graph layout direction"
        },
        "sqlCrack.flowDirection": {
          "type": "string",
          "default": "top-down",
          "enum": [
            "top-down",
            "bottom-up"
          ],
          "enumDescriptions": [
            "Data flows from top to bottom (default)",
            "Data flows from bottom to top (Snowflake query profile style — tables at bottom, results at top)"
          ],
          "description": "Direction of data flow in the visualization. Bottom-up matches Snowflake query profile style where table scans start at the bottom and results flow upward."
        },
        "sqlCrack.autoRefresh": {
          "type": "boolean",
          "default": true,
          "description": "Automatically refresh visualization when SQL file changes (with debounce)"
        },
        "sqlCrack.autoRefreshDelay": {
          "type": "number",
          "default": 500,
          "minimum": 100,
          "maximum": 5000,
          "description": "Delay in milliseconds before auto-refresh triggers (default: 500ms)"
        },
        "sqlCrack.workspaceAutoIndexThreshold": {
          "type": "number",
          "default": 50,
          "minimum": 10,
          "maximum": 500,
          "description": "Maximum number of SQL files to auto-index on workspace analysis (larger workspaces require manual confirmation)"
        },
        "sqlCrack.workspaceLineageDepth": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "maximum": 20,
          "description": "Default lineage traversal depth for Lineage and Table Explorer views"
        },
        "sqlCrack.workspaceGraphDefaultMode": {
          "type": "string",
          "default": "tables",
          "enum": [
            "files",
            "tables"
          ],
          "enumDescriptions": [
            "File-level dependency graph. Nodes are SQL files; edges show which files reference tables defined in other files.",
            "Table-level dependency graph. Nodes are tables/views; edges show references (SELECT, JOIN, INSERT, etc.)."
          ],
          "description": "Default mode for the Workspace Dependencies Graph tab. Used when the panel loads or when you refresh the index."
        },
        "sqlCrack.advanced.cacheTTLHours": {
          "type": "number",
          "default": 24,
          "minimum": 0,
          "maximum": 168,
          "description": "Cache time-to-live in hours. Set to 0 to disable caching. Index will be rebuilt after this time. (Advanced)"
        },
        "sqlCrack.advanced.clearCacheOnStartup": {
          "type": "boolean",
          "default": false,
          "description": "Always rebuild index when VS Code opens the workspace. Overrides cache TTL. (Advanced)"
        },
        "sqlCrack.gridStyle": {
          "type": "string",
          "default": "lines",
          "enum": [
            "dots",
            "lines",
            "none"
          ],
          "enumDescriptions": [
            "Dot grid pattern (subtle dots at intersections)",
            "Line grid pattern (default, subtle grid lines)",
            "No grid pattern (plain background)"
          ],
          "description": "Background grid pattern style for visualizations"
        },
        "sqlCrack.nodeAccentPosition": {
          "type": "string",
          "default": "left",
          "enum": [
            "left",
            "bottom"
          ],
          "enumDescriptions": [
            "Color accent strip on the left edge of nodes (default)",
            "Color accent strip on the bottom edge of nodes"
          ],
          "description": "Position of the color accent strip on visualization nodes"
        },
        "sqlCrack.showMinimap": {
          "type": "string",
          "default": "auto",
          "enum": [
            "auto",
            "always",
            "never"
          ],
          "enumDescriptions": [
            "Show when graph has 2+ nodes",
            "Always show the minimap",
            "Never show the minimap"
          ],
          "description": "Controls when the minimap overview is displayed"
        },
        "sqlCrack.colorblindMode": {
          "type": "string",
          "default": "off",
          "enum": [
            "off",
            "deuteranopia",
            "protanopia",
            "tritanopia"
          ],
          "enumDescriptions": [
            "Use the default palette",
            "Optimize graph colors for deuteranopia (green-red deficiency)",
            "Optimize graph colors for protanopia (red weakness)",
            "Optimize graph colors for tritanopia (blue-yellow deficiency)"
          ],
          "description": "Color accessibility mode for SQL flow and warning visuals. Adds shape/dash cues alongside color."
        },
        "sqlCrack.advanced.defaultTheme": {
          "type": "string",
          "default": "light",
          "enum": [
            "auto",
            "dark",
            "light"
          ],
          "enumDescriptions": [
            "Automatically match VS Code theme",
            "Always use dark theme",
            "Always use light theme (default)"
          ],
          "description": "Default theme for SQL Crack visualizations. 'auto' matches VS Code theme, 'dark' or 'light' forces a specific theme. (Advanced)"
        },
        "sqlCrack.advanced.showDiagnosticsInProblems": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Show SQL Crack parser/performance hints in VS Code Problems panel. Disabled by default to reduce noise; enable when you want lint-like feedback in Problems. (Advanced)"
        },
        "sqlCrack.advanced.showDeadColumnHints": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Show hints for columns selected but not used in WHERE/ORDER BY/GROUP BY/HAVING/JOIN clauses. Disable if you get too many false positives (columns used by application layer). (Advanced)"
        },
        "sqlCrack.advanced.combineDdlStatements": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Combine consecutive DDL statements (CREATE TABLE, CREATE VIEW, etc.) into a single visualization block. Useful for schema files with many DDL statements. Shows count of tables/views created. (Advanced)"
        },
        "sqlCrack.advanced.maxFileSizeKB": {
          "type": "number",
          "default": 100,
          "minimum": 10,
          "maximum": 10000,
          "markdownDescription": "Maximum SQL file size in KB before the parser truncates or rejects the input. Increase for very large SQL files. (Advanced)"
        },
        "sqlCrack.advanced.maxStatements": {
          "type": "number",
          "default": 50,
          "minimum": 1,
          "maximum": 500,
          "markdownDescription": "Maximum number of SQL statements to parse in a single file. Statements beyond this limit are skipped. (Advanced)"
        },
        "sqlCrack.advanced.parseTimeoutSeconds": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "maximum": 60,
          "markdownDescription": "Maximum time in seconds for parsing a single SQL file. Prevents the extension from hanging on very complex queries. (Advanced)"
        },
        "sqlCrack.advanced.debugLogging": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Enable verbose debug logging to the SQL Crack output channel. Useful for troubleshooting. (Advanced)"
        },
        "sqlCrack.advanced.workspaceUxInstrumentation": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Enable local-only Workspace Graph UX interaction metrics in the SQL Crack output channel. No external telemetry is sent. (Advanced)"
        },
        "sqlCrack.customAggregateFunctions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Additional aggregate function names to recognize (e.g., [\"MY_CUSTOM_AGG\", \"COMPANY_SUM\"]). These are added to the built-in dialect functions."
        },
        "sqlCrack.customWindowFunctions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Additional window function names to recognize (e.g., [\"MY_CUSTOM_RANK\"]). These are added to the built-in dialect functions."
        },
        "sqlCrack.additionalFileExtensions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "markdownDescription": "Additional file extensions to treat as SQL files (e.g., `[\".tpt\", \".dbsql\", \".hql\", \".bteq\"]`). Extensions should include the leading dot. These files will show the SQL Crack icon in the editor title bar and can be visualized."
        }
      }
    },
    "walkthroughs": [
      {
        "id": "sqlCrack.getStarted",
        "title": "Get Started with SQL Crack",
        "description": "Learn how to visualize SQL queries, explore workspace dependencies, and trace column lineage.",
        "steps": [
          {
            "id": "openSqlFile",
            "title": "Open a SQL file",
            "description": "Open any `.sql` file in your workspace to get started.\n\n[Open a SQL file](command:workbench.action.quickOpen)"
          },
          {
            "id": "visualizeQuery",
            "title": "Visualize your query",
            "description": "Press `Cmd+Shift+L` (Mac) or `Ctrl+Shift+L` (Windows/Linux) to open the interactive flow diagram.\n\n[Visualize SQL Query](command:sql-crack.visualize)"
          },
          {
            "id": "exploreWorkspace",
            "title": "Explore workspace dependencies",
            "description": "See how your SQL files relate to each other. Right-click a folder in Explorer and choose **Analyze Workspace Dependencies**.\n\n[Analyze Workspace](command:sql-crack.analyzeWorkspace)"
          },
          {
            "id": "columnLineage",
            "title": "Try column lineage",
            "description": "Press `C` while viewing a query visualization to toggle column-level lineage. Click a column to trace its data flow through the query."
          }
        ]
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production",
    "lint": "eslint src --ext ts",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/runJest.js",
    "test:watch": "node scripts/runJest.js --watch",
    "test:coverage": "node scripts/runJest.js --coverage",
    "test:ci": "node scripts/runJest.js --ci --coverage --reporters=default --reporters=jest-junit",
    "precheck": "npm run typecheck && npm run lint && npm run test:ci",
    "prepare": "node scripts/install-hooks.js || true",
    "benchmark": "npx ts-node tests/benchmark/parseBenchmark.ts"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.x",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^6.15.0",
    "@typescript-eslint/parser": "^6.15.0",
    "eslint": "^8.56.0",
    "jest": "^30.2.0",
    "jest-junit": "^16.0.0",
    "process": "^0.11.10",
    "ts-jest": "^29.4.6",
    "ts-loader": "^9.5.1",
    "typescript": "^5.3.3",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "@types/d3-force": "^3.0.10",
    "@types/dagre": "^0.7.53",
    "d3-force": "^3.0.0",
    "dagre": "^0.8.5",
    "node-sql-parser": "5.4.0"
  }
}
